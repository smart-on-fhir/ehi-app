import { request } from "../../src/lib/fetchHelpers";

describe("request", () => {
  test("Failing requests should throw an error", () => {});
  test("JSON Responses should be parsed and returned as an object", () => {});
  test("Text responses should return as a string", () => {});
  test("Responses returning an operation outcome should produce a human readable error message", () => {});
});
